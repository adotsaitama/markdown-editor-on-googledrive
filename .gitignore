# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# local env (never commit real client IDs / secrets)
.env
.env.local
.env.*.local

# Google OAuth credential downloads — MUST never be committed
client_secret_*.json
*.apps.googleusercontent.com.json

# local tool settings
.claude/

# reference clone of the Obsidian theme (tokens are ported into src/themes/)
Blue-Topaz_Obsidian-css/

# logs
npm-debug.log*
*.log

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
